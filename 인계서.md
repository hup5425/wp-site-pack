# wp-site-pack 인계서 (작업 이어받기용)

> **다음 세션(다른 창)에서 이 프로젝트를 이어받는 Claude에게.**
> 이 문서를 **가장 먼저** 읽고, 아래 "지금 바로 할 일"부터 시작하라. 상세 명세는 같은 폴더의 **`기획서.md`** 에 있다. 둘을 함께 읽을 것.

---

## ⏭ 다음 창은 여기부터 (2026-09-15 새벽 · v0.4.2 · 세 사이트 옮김 끝 · zau 는 서버 이전 뒤 다시)

**지금 상태(2026-09-15 01:3x)**
- **v0.4.2 릴리스함**(zip 파일 목록 = v0.4.1 과 같고 `.DS_Store` 만 빠짐. zip 은 이제 `-x "*.DS_Store" "wp-site-pack/tools/*"` 로 만든다).
- **coreabiz · apt-view · benefitf 끝**: 사이트팩 0.4.2 설치 → SEO 설정 가져오기 → Rank Math 끄기 → 대조 「후」 저장(`tools/대조기록/<도메인>_후.json`) → 삭제까지 했다.
  - 지운 것: 세 곳 모두 Rank Math 무료·프로 · Bing Webmaster · (apt-view·benefitf) Instant Indexing · (apt-view) **알팩 · MWW**. 켜진 모듈: seo · ads_manager · auto_index · scheduled_publish (+apt-view header_footer).
  - coreabiz 는 「글 종류」를 **NewsArticle** 로 두었다(Rank Math 프로가 글마다 NewsArticle 을 내던 것 그대로).
  - 대조에서 남은 차이(뜻이 같거나 나아진 것): 발행자 Person→Organization · 로봇 메타 순서 · 보관함 CollectionPage 이름 칸 없음 · 검색·작성자 canonical 생김 · 홈 제목(apt-view `아파트VIEW – https://apt-view.com` → `아파트VIEW`, benefitf `home` → `Benefitⓕ`) · 첨부파일 404→301 · benefitf 카테고리 제목에 `• Benefitⓕ` 붙음 · 사이트맵 쪽 나눔 순서만 바뀜(주소 수 같음).
  - 🔴 coreabiz 의 **news-sitemap.xml · video-sitemap.xml 은 없어졌다**(Rank Math 프로 기능). 서치콘솔·서치어드바이저에 그 주소가 제출돼 있으면 빼야 한다.
  - apt-view 알팩을 지우기 전·후 홈과 최신글의 script·stylesheet 목록이 **똑같았다**(애드센스·gtag 는 알팩이 아니라 Ad Inserter 몫).
- **zau.kr**: 0.4.2 설치·Rank Math 삭제를 했는데 **옛 서버(bcbnews2)에 들어갔다.** 서버 관리 창이 그날 밤 bcbnews2 → apt-view 서버로 9곳(bcbnews·wooun·musick·lawyersong·rewrite·zau·licen·bandaimobile·ctrla)을 옮기는 중이었고, 옛 서버를 다시 복사하지 않으므로 **새 서버에는 이 변경이 없다.**
  → 서버 관리 도구 `.sites.json` 에서 zau.kr 의 hosting 이 `cloudways2` 로 바뀐 뒤(`서버 관리/데이터/이전_20260914/ssl8.log`) **zau 에 다시**: `옮기기.py 준비 zau.kr v0.4.2` → Rank Math 무료판 끄기·지우기. zau 는 「전」 기록이 없으니 대조 없이(0.4.2 로 이미 확인함).
- 서버 관리 창의 부탁(2026-09-15): apt-view 서버에서는 **wp-cli 를 한 사이트씩**, **서버 단위 Cloudways API(권한 초기화·SSL·cname)는 부르지 않는다.**

- **v0.4.3 릴리스·설치(coreabiz·apt-view·benefitf)**: 워프글쓰기 「웹마스터 도구」용 REST `includes/class-rest.php` — `GET wsp/v1/webmaster`(버전·모듈·robots 다음 줄·네이버 메타) · `POST /webmaster/daum {line}`(Ads 매니저 `put_daum_line` → 캐시 비우기) · `POST /webmaster/naver {verify}`(자동 인덱싱 `put_naver_verification`) · `POST /cache/purge`(breeze_clear_all_cache·varnish·객체 캐시). 관리자 앱 비밀번호만(익명 401 확인). 워프글쓰기 인계서: `wp-auto-writer/docs/워프/인계서_2026-09-15_웹마스터도구.md`.

**✅ 2026-09-15 오후 — 서버 관리 창: v0.4.6 「소유 확인·분석 코드」 모듈(사장님 지시)**
- 사장님 결정(2026-09-15): ① 소유 확인은 IndexNow 와 관계없으니 **따로 떼어 「소유 확인」임을 알 수 있게** ② 이미 사이트에 심은 인증 코드·파일을 **찾아서 칸에 자동 표시, 없으면 없다고** ③ **GA4·애드센스·네이버 애널리틱스·클래리티도 신설·자동 반영** ④ GeneratePress 엘리먼츠에 심어 둔 이 코드들은 **사이트팩으로 옮기고 엘리먼츠를 끈 뒤, 캐시 비우고 확인해서 정상이면 삭제** ⑤ 인증 태그도 사이트팩으로(인증 HTML 파일은 웹 루트에 그대로) ⑥ 모듈 하나에 칸 두 개 ⑦ 구글 인덱싱 칸에 **키 발급 단계·링크 + 「일반 글엔 효과 거의 없음」 경고**
  ⑧ **릴리스는 묻지 말고 자체 시험(한 사이트 먼저) 뒤 알아서**.
- 코드: `includes/modules/class-mod-site-codes.php`(값만 넣으면 표준 코드 생성 · `find_codes()`·`clean_*()` 순수 함수 · `scan_sources()` = 훅 엘리먼츠 `_generate_element_content` · 자식 테마 functions.php · Insert Headers and Footers(켜진 경우만) · 사이트팩 헤더&푸터 · 방문자통계 `ga_enable` · 칸마다 「사이트팩이 넣는 중 / 첫 화면에 N번 / 다른 곳에도 있음」 · 「찾아서 빈 칸 채우기」 단추).
  `WSP_Core::boot()` 가 `WSP_Mod_Site_Codes::maybe_migrate()` 로 **한 번** auto_index·ads_manager 의 인증 값·인증 파일을 옮기고 site_codes 를 켠다(`wsp_site_codes_migrated`). REST `webmaster/naver` 는 site_codes 로(주소 그대로). 검산 `tools/site_codes_검산.php` 49개.
- 🔴 **GeneratePress 훅 엘리먼츠의 코드는 `post_content` 가 아니라 메타 `_generate_element_content`** 에 있다(처음 조사에서 `post_content` 만 봐서 0개로 나왔다).
- 21곳 조사(2026-09-15): GA4·클래리티·인증·애드센스 로더는 거의 전부 훅 엘리먼츠 · **네이버 애널리틱스는 bankserch·benefitf·lgforyou·ssometime·bandaimobile·rewrite 가 자식 테마 functions.php**(`add_naveranalytics` 블록, 6곳 같은 모양) · bcbnews GA4 는 방문자통계 `ga_enable=1`.
  광고 단위(`<ins>` 포함)·카카오 공유·SNS 버튼·쿠팡 포워딩·화면 확대 막기 엘리먼츠는 **추적 코드가 아니라서 건드리지 않는다**.
- 시험: bankserch.com 에 개발판 → 찾기 5칸 전부 맞음 → 옮기기(엘리먼츠 4개 끄기·functions.php 블록 제거) → 첫 화면·글 코드 수 전=후 → 엘리먼츠 삭제. 이후 v0.4.6 릴리스 · 21곳 설치 · 20곳 옮기기(서버 관리 창 scratchpad `migrate_codes.py`, 백업·로그 `~/클로드작업/서버 관리/데이터/코드이전_20260915/`).
- ✅ **21곳 옮기기 끝** — 최종 점검: 코드마다 첫 화면 한 번씩(애드센스는 광고 단위 몫까지 전과 같음) · 추적 코드 든 훅 엘리먼츠 0 · 자식 테마 네이버 블록 0 · 방문자통계 GA4 출력 0. (musick 의 「애드센스」 엘리먼츠 #626 은 **옮기기 전부터 임시글**이라 그대로 둠.)
- 🔴 **v0.4.7**: coreabiz 는 머리말 로더 엘리먼츠 없이 **푸터 광고 단위(cobiz_footer, `<ins>`)만** 로더를 내고 있었는데, 찾기가 그 ID 로 사이트팩 칸을 채워 로더가 2번 → `without_ad_unit_loader()` 로 **`<ins>` 가 든 엘리먼츠에서는 애드센스를 찾지 않게**(검산 +3, 52). coreabiz 에서 개발판 시험(찾기 애드센스 0 · 코드 수 전=후) 뒤 릴리스·21곳 설치.

**✅ 2026-09-15 11시~ — 서버 관리 창: v0.4.4 릴리스 · 17곳 옮기기(사장님 지시 「필수 기능 전부 켜」 · 「옮기기 진행해」)**
- **v0.4.4 릴리스·푸시**(사장님 허락): SEO 설정 화면 로고·기본 공유 사진 칸 미리보기(`image_preview()` + admin.js `.wsp-img-preview`). zip 파일 목록 v0.4.3 과 같음. 검산 104 통과.
- **필수 모듈 5개**(scheduled_publish · auto_index · ads_manager · seo · **ad_protector**)를 21곳 전부에: 이미 옮긴 4곳은 ad_protector 만 추가 + 0.4.4.
  나머지 17곳은 `옮기기.py` 절차를 스크립트로(`서버 관리` 창 scratchpad `mig17.py` — 곳마다 전 저장 → 준비 v0.4.4 → ad_protector → 끄기 → robots 사이트맵 줄 확인 → 후 저장 → diff, 로그 `tools/대조기록/<도메인>_옮기기로그.txt`). **지우기는 diff 를 사람이 본 뒤 따로.**
- 🔴 `tools/대조.py` 가 한글 슬러그 주소에서 `UnicodeEncodeError` 로 멈췄다(bcbnews) → `_ascii_url()` 로 퍼센트 인코딩해서 요청.
- 🔴 **Rank Math 가 워드프레스 기본 사이트맵을 쓰던 곳**(bcbnews: robots `wp-sitemap.xml`)은 사이트팩 SEO 가 켜지면 `wp-sitemap.xml` → 301 → `sitemap_index.xml`. 「전」 robots 를 그대로 담으면 사이트맵 줄이 301 을 가리킨다 → **줄이 200 이 아니면 `sitemap_index.xml` 로 바꾸게** 했다(네이버는 넘기는 사이트맵을 수집 제한).
- bcbnews 대조: 확인 필요 282 = 생김 225(og·ldjson·canonical — Rank Math 가 원래 아무것도 안 내던 곳) · 없어짐 4(기본 사이트맵 목록) · 바뀜 23(글 제목 뒤 「– 사이트명」 빠짐 · 작성자 noindex · 첨부 301 · robots 메타에 index,follow 추가).
- ✅ **지우기 18곳 끝(13:2x)** — 17곳 + zau(캐시만): Rank Math 무료·프로 · fast-indexing-api · bing-webmaster-tools · scheduled-post-trigger 삭제(데이터 지움 off 확인) + `uploads/rank-math/rank_math_*.xml` 캐시·`rank_math_sitemap_cache_files` 옵션 삭제. 곳마다 첫 화면·robots·사이트맵 200 · 글 스키마 유지. **21곳 모두 Rank Math 없음.**
- ✅ **17곳 옮기기 끝(11:3x~13:0x)** — bcbnews · se.sdgn · real.apt-view · info.benefitf · wooun · musick · lawyersong · licen · rewrite · bandaimobile · co.coreabiz · lgforyou · bankserch · ctrla · sdgn · ssometime · ssomeday.
  곳마다 모듈 5개 켜짐 · 첫 화면 200 · **사이트맵 빠진 주소 0**(하위 파일 사이에서 자리만 바뀜, 페이지·카테고리 사이트맵이 새로 생김) · 설명 안 되는 차이 0(브레드크럼 단계 수·첨부 301·작성자 noindex 등은 사이트팩 정상 동작).
  점검 스크립트: 서버 관리 창 scratchpad `post17.py`(글 종류 맞추기·사이트맵 집합 비교·설명 안 되는 차이만) · `del17.py`(지우기 + Rank Math 사이트맵 캐시 파일 삭제 + 확인).
  - 🔴 **Rank Math 프로가 글마다 NewsArticle 을 내던 곳**(se.sdgn · real.apt-view · rewrite · bandaimobile · co.coreabiz · lgforyou · bankserch · sdgn · ssomeday)은 가져오기가 `pt_post_default_article_type`(BlogPosting)을 담는다
    → coreabiz 때와 같은 기준으로 **「글 종류」를 NewsArticle 로 맞춤**(전에 나가던 종류 그대로).
  - 🔴 대조.py 「전」 사이트맵이 **0개로 잡힌 곳**(wooun·musick·lawyersong·licen·rewrite·bandaimobile) — Rank Math 사이트맵 **파일 캐시가 하위 사이트맵을 복제 임시 주소**(`wordpress-1667581-<앱id>.cloudwaysapps.com`)로 안내해서 인증서 오류로 못 읽었다. 이전 사고(서버 관리 인계서) — 사이트팩으로 옮기며 새로 만들어 해결.
- 🔴 **결함 3: 수정 시각이 발행 시각보다 이르게 나갔다**(ctrla·ssometime·wooun 대조) — 예약 발행 글은 써 둔 시각(post_modified)이 발행보다 앞선다. Rank Math 는 늦은 쪽을 냈다.
  → `WSP_SEO_Head::post_iso()` 가 수정 시각이 발행보다 이르면 발행 시각을 돌려주게 · 사이트맵 `latest_modified()` 전체 찾기는 수정·발행 내림차순 한 편씩 보고 늦은 쪽. 검산 +2(106 통과). ✅ **v0.4.5 릴리스·푸시 끝**(사장님 허락, 2026-09-15 13:3x · zip 파일 목록 v0.4.4 와 같음). 21곳은 자동 업데이트로 받는다 — 하루 뒤 버전 확인.
- 사장님(2026-09-15): **robots 의 다음 인증코드는 신경 쓰지 말고 기본 robots 만 잘 옮겨라** — 다음 코드는 워프글쓰기 「웹마스터 도구」 메뉴에서 전부 점검한다. 웹마스터 도구에는 21곳 모두 등록(워프글쓰기 앱에는 자동 글쓰기 도는 곳만).

**✅ 2026-09-15 10:2x~10:4x — 서버 관리 창이 한 것(사장님 지시)**
- **zau.kr 다시 끝**: 새 서버에 0.4.3 설치 → 옛 서버(bcbnews2) 의 `wsp_mod_seo`·`wsp_seo_migrated` 를 그대로 복사(나머지 wsp 옵션은 원래 같았음, 옛=새 MD5 일치) → Rank Math 무료판 삭제(데이터 지움 off 확인). 첫 화면·robots·sitemap 200, `/category/auto/` 제목 `auto - 다시쓰기`.
- **사이트팩 0.4.3 을 21곳 전부에 설치**(사장님: "모든 사이트에 사이트팩이 설치되지 않은 곳이 있다면 설치해줘"). `wp plugin install <v0.4.3 릴리스 zip> --force --activate --skip-plugins --skip-themes` 를 한 곳씩.
  **Rank Math 옮기기(준비·끄기·지우기)는 안 했다** — 17곳은 모듈 전부 꺼진 채 Rank Math 가 그대로 SEO 를 한다. 17곳에 옮기기를 넓힐지는 여전히 사장님께.
  - co.coreabiz 는 플러그인 폴더는 없는데 7월 시험 때 켠 `wsp_active_modules`(header_footer·scheduled_publish·auto_index·ads_manager·social_share·scroll_popup)가 DB 에 남아 있어, 설치 순간 되살아나지 않게 **`{}` 로 비움** — 백업 옵션 `wsp_active_modules_backup_20260915`, 파일 `~/클로드작업/서버 관리/데이터/사이트팩설치_20260915/`.
  - licen.kr 의 9/15 00:12 변경은 옛 서버의 **자동 업데이트**(0.3.2→0.4.1)였다(사람 작업 아님).
- 설치를 wp-cli(master)로 해서 폴더 주인이 master 가 됨 → 서버 관리 창이 곧바로 21곳 `reset_permissions` 로 앱 유저 소유로 되돌림(안 하면 다음 자동 업데이트가 폴더를 못 바꾼다).

- 🔴 **결함 2건(2026-09-15, 워프글쓰기 자동 등록에서 드러남) — 코드 고칠 것**
  1. **`put_daum_line()` 이 모듈 꺼진 사이트에서 엉뚱한 바탕을 쓴다.** wooun.kr(모듈 전부 꺼짐·저장 robots 없음·물리 파일 없음) 에서
     `live_robots_txt()` 가 돌려준 값이 Rank Math 의 robots(`Sitemap: …/sitemap_index.xml`)가 아니라 `Allow: /` + `Sitemap: …/sitemap.xml`(404) 였고,
     그게 그대로 저장돼 서비스됐다. transient `wsp_cur_robots` 에 이미 **새 다음 줄이 든 그 내용**이 들어 있었다 — 루프백으로 읽은 값이 Rank Math 결과가 아니었다.
     서버 관리 창이 데이터만 바로잡음: Rank Math `robots_txt_content` + 새 다음 줄로 `wsp_mod_ads_manager.robots_txt` 교체(옛 값 `/home/master/_backups/wooun_wsp_robots_before_20260915.txt`).
     → Rank Math 가 켜져 있으면 `rank-math-options-general['robots_txt_content']` 를 먼저 바탕으로 쓰고, 루프백 결과는 사이트맵 줄이 실제 200 인지 확인하게.
  2. **REST `GET wsp/v1/webmaster` 의 `sitemap` 은 `home_url('/sitemap_index.xml')` 고정값**이다. bcbnews.kr 은 실제 사이트맵이 `wp-sitemap.xml`(sitemap_index 404) — robots 의 Sitemap 줄이나 실제 응답으로 정할 것.

**다음에 할 일**
1. ~~zau.kr 다시(위) — 0.4.3 으로.~~ → 끝(위).
2. wp-auto-writer 발행 때 `meta` 로 `_wsp_seo_focus_keyword`(글 키워드) 보내기 — 사이트팩이 REST 에 등록해 두었다. 사이트팩이 없는 사이트에서는 워드프레스가 모르는 meta 를 그냥 버린다. FAQ 는 **rank-math/faq-block 그대로 둬도 된다** — 사이트팩 SEO 가 그 블록을 읽어 FAQPage 를 낸다(세 사이트 대조에서 FAQPage 4개 확인).
3. 나머지 17곳에 사이트팩을 넓힐지는 사장님께.
4. 총괄 관리 화면은 `~/클로드작업/서버 관리`.

---

## (지난 절) 2026-09-14 밤 · v0.4.1 릴리스 · zau.kr 에서 Rank Math 끄고 시험 중

**🔴 창을 닫을 때 끝나지 않은 것 — 먼저 확인할 것**
- ✅ **v0.4.2 수정은 끝나서 커밋·푸시까지 했다(릴리스·zau 반영은 안 함)** — 검산 104개 통과·`php -l` 통과. 다음 창은 아래 "버전 0.4.2 → 릴리스" 부터. 고친 것 넷: ①보관함·검색 제목 구분 기호가 `–` 로 나감 → `pre_get_document_title` 로 완성 제목 돌려주기(사이트명 자리는 블로그 이름, 검색은 `검색어 - 블로그 이름`, 404 는 `페이지를 찾을 수 없음 - 블로그 이름`) ②첨부파일 주소 404 → `wp_attachment_pages_enabled=0` 이어도 부모 글/홈으로 301 ③「홈 제목에 태그라인 붙이기」(`home_title_tagline`) 칸 + Rank Math `homepage_title` 에 `%sitedesc%` 있을 때만 1 로 이어받기(apt-view 태그라인이 주소라 붙으면 안 됨) ④`title_separator` 의 HTML 엔티티 풀기(benefitf `&bull;`).
- 파일: `includes/modules/class-mod-seo.php` · `includes/modules/seo/class-seo-head.php` · `tools/seo_검산.php`. **`git status` 로 반쯤 고친 흔적부터 본다** → `php -l` 전부 · `php tools/seo_검산.php` → 넷이 다 들어갔는지 코드로 확인 → 모자라면 마저 → (버전 0.4.2·커밋·푸시는 끝남) → zip·`gh release create v0.4.2`(CLAUDE.md 절차, 지난 zip 과 파일 목록 diff).
- 그다음 zau: `$PY -c` 대신 `wp plugin install https://github.com/hup5425/wp-site-pack/releases/download/v0.4.2/wp-site-pack-v0.4.2.zip --force` → 설정 가져오기 다시(`import_rank_math` 는 "기본값인 칸에만" 이라 home_title_tagline 을 넣으려면 zau 는 `%sitedesc%` 가 있으니 1 그대로) → `curl https://zau.kr/category/auto/?x=1` 제목이 `auto - 다시쓰기` 인지 · `curl -I https://zau.kr/3095/` 가 301 인지.
- 그 뒤 zau Rank Math 삭제 → 세 사이트는 `tools/옮기기.py`(아래)로 `준비 → 끄기 → 대조 후 저장·diff → 지우기`. 「전」 기록은 이미 `tools/대조기록/` 에 있다(Rank Math 켜진 상태, 21:42 저장).
- 사장님이 정하신 범위(2026-09-14): zau 에서 다 확인되면 **네 사이트 전부 설치하고 Rank Math·알팩·MWW·Instant Indexing·Bing 플러그인은 다음 창이 알아서 전부 삭제**해도 된다(사장님: "너가 다 알아서 모든사이트에 설치하고, 또 모든사이트에서 지워"). 구글 인덱싱 API 키는 다시 만들지 않는다(일반 글엔 효과 없음).

**지금 상태(2026-09-14 21:5x)**
- **릴리스**: v0.4.0(SEO 모듈·구글 인덱싱 API·결함 15건) → v0.4.1(zau 실측 수정 9가지). GitHub `hup5425/wp-site-pack` 공개 저장소. 커밋·푸시·릴리스는 사장님 허락 받은 것(2026-09-14).
- **zau.kr 실제 상태**: 사이트팩 **0.4.1** 활성, 켜진 모듈 = `seo` · `ads_manager`(robots.txt 에 사이트맵·다음 인증 줄을 담아 둠 — Rank Math 가 만들던 것을 옮김). **Rank Math 무료판은 껐다(삭제 아님)**. SEO 설정은 Rank Math 에서 이어받아 사이트 이름=`zau` · 조직 이름=`zau` · 글 종류=BlogPosting. 사이트맵 `/sitemap_index.xml` 정상(글 343편 전부 — Rank Math 것은 캐시가 낡아 275편만 담고 있었다). Bing Webmaster Url Submission 플러그인은 아직 켜져 있음(지울 것). MWW·Instant Indexing 은 zau 에 없음.
- **실사이트 작업 방법**: 서버 관리 도구 `~/클로드작업/wp-visitor-stats/_local-tool/`(`.venv/bin/python` 필수) 의 `wpcli._ssh`/`_run` 으로 SSH+WP-CLI. 설치는 `wp plugin install <GitHub 릴리스 zip 주소> --force`(SFTP 업로드는 권한 거부). 모듈 켜기 `wp option patch insert wsp_active_modules <슬러그> 1` + `wp rewrite flush`. 사장님이 "클라우드웨이즈 API 로 네가 다 해라" 하심.
- **대조 도구**: `python3 tools/대조.py 저장 <주소> <파일>` / `diff 전 후`. zau 「전」(Rank Math 켜진 상태)은 scratchpad 에 있었는데 사라졌을 수 있다 — Rank Math 를 다시 켜서 저장하지 말 것(끄고 켜기 반복하지 않는다). 남은 세 사이트는 옮기기 전에 「전」부터 저장.
- **2차 대조에서 남은 차이(고치는 중, 소넷)**: ①보관함 제목 구분 기호가 `–` 로 나감(워드프레스 `wptexturize` 탓 → `pre_get_document_title` 로 완성 제목을 돌려주는 방식으로) ②첨부파일 주소 404(`wp_attachment_pages_enabled=0` 이라 워드프레스가 404 → 그래도 홈/부모 글로 301 시키게). 고쳐지면 **v0.4.2** 로 릴리스하고 zau 에 `wp plugin install … --force` 로 올린 뒤 `/category/auto/` 제목과 `/3095/` 응답을 확인.
- 나머지 차이는 뜻이 같거나 나아진 것: 로봇 메타 순서 · Person→Organization · 트위터 label 삭제 · 홈 og:image 생김 · 날짜 시간대 +09:00.

**다음에 할 일(순서대로)**
1. v0.4.2 릴리스·zau 반영·확인(위).
2. zau 에서 **예약글 발행 보장** 모듈 켜고 예약글 하나로 시각에 나가는지 확인, **자동 인덱싱** 모듈 켜고 IndexNow 키 파일 200 확인. 되면 zau 의 Bing 플러그인 삭제(`wp.plugin_delete` 또는 `wp plugin delete bing-webmaster-tools`).
3. zau 에서 Rank Math **삭제**(`wp plugin delete seo-by-rank-math`) — 그 전에 Rank Math 의 "삭제 시 데이터 지움" 설정이 꺼져 있는지 `wp option get rank-math-options-general` 에서 `remove_data_on_uninstall` 확인(`off` 여야).
4. coreabiz · apt-view · benefitf: 각각 「전」 저장 → `wp plugin install`(0.4.x) → `wsp_active_modules` = seo·ads_manager(+robots 내용을 그 사이트 「전」 robots 그대로) → SEO 모듈 `import_rank_math` 결과를 `wsp_mod_seo` 에 저장(zau 에서 한 방식: `wp eval` 로 결과 받아 `wp option update`) → coreabiz 는 글 종류 NewsArticle 인지 확인 → Rank Math 무료·프로 끄기 → 「후」 diff → 문제 없으면 Rank Math 둘·알팩(apt-view)·MWW·Instant Indexing(`fast-indexing-api`)·Bing 삭제. apt-view 는 알팩이 소셜 공유·관련 글을 그리고 있을 수 있으니 지운 뒤 사이트팩 모듈로 대신할지 사장님께.
5. 구글 인덱싱 API: 사장님께 보고한 대로 **일반 글에는 효과가 없어 키를 다시 만들지 않기로**(구글이 채용공고·생방송용으로만 인정, 2025-05 부터 강제). 기능은 남겨 두고 끔.
6. wp-auto-writer 쪽: 발행 때 `meta` 로 `_wsp_seo_description`·`_wsp_seo_focus_keyword` 보내기, FAQ 를 rank-math 블록 대신 제목형으로(전 사이트 0.4.x 뒤).
7. 총괄 관리 화면은 `~/클로드작업/서버 관리` 프로그램에(사장님 결정).

**세 사이트 실측(2026-09-14 21:4x, WP-CLI 읽기만)** — 「전」 대조 기록은 `tools/대조기록/<도메인>_전.json`(Rank Math 켜진 상태, 커밋 안 함)
| | coreabiz.com | apt-view.com | benefitf.com |
|---|---|---|---|
| 사이트팩 | 없음(옵션만 남음: scheduled_publish) | 없음(옵션만: header_footer·scheduled_publish·auto_index) | 없음 |
| 지울 플러그인 | Rank Math·프로 · Bing | Rank Math·프로 · **alpack** · **fast-indexing-api**(Instant Indexing) · **scheduled-post-trigger**(MWW) · Bing | Rank Math·프로 · fast-indexing-api · Bing |
| 예약글 | **14편**(워프 예약) — 사이트팩 켜면 예약발행 보장이 바로 돈다(시각 지난 것만 발행, 정상) | 0 | 0 |
| Rank Math 사이트 이름 / 조직 | 코리아비즈 / coreabiz(로고 있음) | 아파트VIEW / 아파트VIEW(로고 있음) · 다른 이름 apt-view | BenefitF / BenefitF |
| 홈 제목 규칙 | `%sitename%` | `%sitename% %page%` (태그라인이 `https://apt-view.com` 이라 붙으면 안 됨) | `%sitename% %page%` |
| 구분 기호 | - | - | `&bull;` (엔티티 — 풀어서 저장해야) |
| 첨부파일 페이지(워드프레스) | 켜짐(1) | 꺼짐(0) | 꺼짐(0) |
| IndexNow 키 | 알팩 키·Rank Math 키 둘 다 옵션에 있음 · 루트에 키 파일 **없음**(가상 서빙 필요) | 같음 | 같음 |
- coreabiz 글에 `NewsArticle` 이 나가는데 Rank Math 설정은 BlogPosting — 프로판의 글별 스키마일 수 있다. 옮긴 뒤 대조에서 글 종류 차이가 나면 「글 종류」를 NewsArticle 로 둘지 사장님께.
- 홈 제목 태그라인 조건·`&bull;` 풀기는 v0.4.2 에 넣는 중(소넷).
- zau.kr: 자동 인덱싱(알팩 키 `presslearn_indexnow_api_key` 이어 씀, 루트 키 파일 200) · 예약글 발행 보장 켬. 예약 시험 글 ID 50997 이 **예약 시각에 발행됨**(21:44:39) · 자동 인덱싱이 그 글을 **IndexNow 202** 로 보냄 → 시험 글 **삭제함**. zau 의 **Bing Webmaster Url Submission 삭제함**(21:4x). Rank Math 무료판은 **꺼 둔 채 남김** — v0.4.2 확인 뒤 삭제.
- **옮기기 도구 `tools/옮기기.py`**(서버 관리 도구의 `.venv/bin/python` 으로): `상태` · `준비 <도메인> <태그>`(설치·Ads 매니저 robots=「전」 기록 줄·자동 인덱싱 키 이어받기·예약발행·SEO 설정 가져오기) · `끄기`(Rank Math 둘) · `되돌리기` · `지우기`(Rank Math 둘·alpack·fast-indexing-api·scheduled-post-trigger·bing-webmaster-tools, "삭제 시 데이터 지움" 켜져 있으면 멈춤). zau 에 `상태` 로만 돌려 봄.

**이전 상태(2026-09-14 낮, v0.4.0 만들 때)**
- 사장님 결정(2026-09-14): 사이트팩이 **Rank Math(무료·프로)·알팩·MWW Scheduled Post Trigger·Instant Indexing·Bing Webmaster Url Submission 을 전부 대신**하고 그 플러그인들은 삭제. **시험은 zau.kr 에서 전부**, 끝나면 coreabiz·apt-view·benefitf 에 배포. 총괄 관리(네 사이트 한 번에)는 나중에 `~/클로드작업/서버 관리` 프로그램에 붙인다.
- 실측(2026-09-14): 사이트팩 파일은 **zau.kr 에만** 있고 거기서도 꺼져 있다. 다른 세 사이트에는 폴더 자체가 없다(아래 §5 의 "통계 1.2.67 이 전 사이트 자동 설치" 는 실제로 안 됐다 — 왜인지 확인 필요). apt-view 에는 알팩이 돌고 있다.
- 오늘 한 것(전부 로컬, 커밋 전): ①전수조사에서 나온 결함 15건 수정(가상 서빙 404·엔터로 인증 파일 삭제·애드 프로텍터가 캐시와 충돌·통계 연동 함수명 불일치·IP 헤더 신뢰·일괄 인덱싱 1,000편 제한·크론 안 지워짐·로그가 설정 옵션에 섞임·ads.txt 이스케이프·robots 기본값·인증 파일 두 곳·꺼진 모듈 단추 무반응·카카오 사진·uninstall.php·readme) ②**SEO 모듈**(`includes/modules/class-mod-seo.php` + `seo/` 4파일, 명세 = 기획서 4.8) ③자동 인덱싱에 **구글 인덱싱 API**(기획서 4.2 추가분) ④**대조 도구 `tools/대조.py`**(Rank Math 끄기 전후 비교; zau.kr 「전」은 `/tmp/zau_전.json` 에 저장돼 있으나 /tmp 라 사라질 수 있음 — 다시 `저장` 하면 된다) ⑤검산 `tools/seo_검산.php`(48) · `tools/google_index_검산.php`(32) 통과. `php -l` 전부 통과. 버전 0.4.0 으로 올림.
- **실제 워드프레스에서 켜 본 적은 아직 없다.** 실사이트 검증 0.

**다음에 할 일(순서대로)**
1. 커밋·푸시·릴리스 v0.4.0 (사장님 허락 받고).
2. zau.kr: 대조 도구 `python3 tools/대조.py 저장 https://zau.kr 전.json` → 사이트팩 업데이트·모듈 켜기(각 모듈 확인 순서는 오늘 담당별 보고에 있고, 요지는 기획서 4.8 "옮기는 순서") → Rank Math 둘 **끄기**(삭제 아님) → `저장 후.json` → `diff` 가 "확인 필요 0" 이면 다음.
3. 예약글 발행 보장 실전 확인(예약글이 시각에 나가나) → MWW 삭제. 구글 인덱싱 API 는 사장님이 서비스 계정 키를 만들어 붙여 넣어야 돈다(콘솔 5단계는 오늘 보고에).
4. Rank Math 삭제할 때 **"삭제 시 데이터 지움" 을 끈 채** 지운다(글별 설명문·포커스 키워드가 `rank_math_*` 메타에 있고 SEO 모듈이 읽어 온다).
5. 나머지 세 사이트: 사이트팩 설치(자동 설치가 안 된 까닭부터) → 같은 순서. apt-view 알팩 삭제. coreabiz 는 「글 종류」를 NewsArticle 로.
6. wp-auto-writer 쪽: 발행 때 `meta` 로 `_wsp_seo_description`·`_wsp_seo_focus_keyword` 보내기, FAQ 를 rank-math 블록 대신 제목형으로 쓰기(전 사이트가 v0.4.0 이 된 뒤).

**막힌 자리·주의**
- readme.txt 는 네 사이트 모두 403 이라 밖에서 버전을 못 읽는다. 플러그인 유무는 `assets/front/*.js` 200/404 로 본다.
- 네 사이트 모두 Breeze+CDN 이 HTML 을 약 25분 캐시한다. 확인할 땐 주소에 쿼리(`?x=1`)를 붙인다(x-cache MISS).

---

## 0. 30초 요약
- **무엇:** 알팩(AlPack) 스타일의 **모듈형 통합 워드프레스 플러그인**을 새로 만든다. 폴더 `/Users/hupark/클로드작업/wp-site-pack/` (가칭).
- **왜 새 플러그인:** 통계 플러그인(`wp-visitor-stats`)은 그대로 두고, 유틸 기능은 성격이 달라 분리. On/Off 모듈 구조로 격리·경량화. (결정 근거는 기획서 §1.)
- **범위:** 모듈 **7개** — 헤더&푸터 / 예약글 발행 보장 / 자동인덱싱(IndexNow) / Ads매니저(ads.txt) / 소셜공유 / 스마트스크롤(팝업) / 애드프로텍터.
- **사용자 방침:** "일단 전부 다 만들고, 그 뒤 내가 하나씩 검토하며 수정." → 7개 모듈을 모두 구현하되 각각 독립 토글.
- **현재 상태:** **기획·문서만 완료. 코드는 아직 0줄.** (이 폴더엔 `기획서.md`, `인계서.md` 만 있음.)

---

## 1. 확정된 결정 사항
1. **아키텍처:** 새 모듈형 팩. 활성 모듈만 런타임 훅 등록(레지스트리 `WSP_Core` + 추상 `WSP_Module`). 기획서 §3.
2. **모듈 저장:** 모듈별 옵션 `wsp_mod_{slug}` + 활성목록 단일 옵션 `wsp_active_modules`.
3. **통계 연동:** `class-stats-bridge.php`로 `wp-visitor-stats` 데이터(IP·국가·봇·애드센스)를 **읽기 전용 재사용**. 없으면 각 모듈 자체 폴백. 통계 DB에 쓰지 않음.
4. **배포:** GitHub 릴리스 + 자체 업데이터(통계 `class-updater.php` 복사·개조). 버전 두 곳 일치(`Version:` + `WSP_VERSION`). 구버전 zip 자동 정리.
5. **UI:** 알팩식 카드 대시보드 + 행(row) 설정폼. PHP 렌더 + 소량 JS/CSS. 무거운 프레임워크 미사용.
6. **네이밍:** 폴더 `wp-site-pack`, 접두어 `wsp_`, 상수 `WSP_`, 클래스 `WSP_*`, 메뉴 "사이트 팩" — **모두 가칭.** 브랜드 확정 전까지 이대로 진행하되 한 곳에 상수화해 나중에 일괄 변경 쉽게.

## 2. 아직 사용자에게 확인해야 할 것 (막히면 물어볼 것)
- 브랜드/플러그인 최종 이름.
- GitHub 저장소 신규 생성(비공개+토큰?) 및 자동배포 여부.
- 외부 키: IndexNow 키, 카카오 JS 키, CloudFlare 토큰 — 확보/입력 주체.
- robots.txt/ads.txt/인증파일: 가상 서빙만으로 갈지(대개 충분).
- 예약글 발행: 외부 핑을 **허브(apt-view.com)** 가 대신 때리게 할지(가장 확실).
- 기존 유틸(링크체크·용량최적화)을 이 팩으로 흡수할지(장기).
> 사용자는 **비개발자**다. 한글로, 쉬운 말로 설명하고, 바깥으로 나가는 작업(깃 푸시·릴리스·파일 실배포)은 **먼저 확인** 후 진행.

---

## 3. 지금 바로 할 일 (다음 세션 시작점)
> 아래 순서대로. 각 단계 끝에 §5 체크리스트를 갱신할 것.

**STEP 0 — 착수 전:** `기획서.md` + 이 문서 정독. 사용자에게 "이어서 스캐폴딩부터 시작하겠다" 알림(바깥 작업 아님, 바로 진행 가능).

**STEP 1 — 플러그인 뼈대(스캐폴딩):** *(가장 중요 — 여기서 프레임워크가 서면 나머지는 반복 작업)*
- `wp-site-pack.php`(헤더·상수 `WSP_VERSION`/`WSP_DIR`/`WSP_URL`·오토로드·부트스트랩·활성화훅).
- `includes/class-module.php`(추상), `class-core.php`(레지스트리: 모듈 등록 → 활성만 `register()`).
- `includes/class-settings.php`(옵션 get/set/기본값 병합).
- `includes/class-admin.php` + `admin/dashboard.php`(카드 그리드 + 모듈 On/Off 토글 저장) + 모듈 설정 라우팅.
- `assets/admin.css`/`admin.js`(카드·토글·행 폼 UI — 통계 톤).
- **검증:** 워드프레스에 설치 → "사이트 팩" 메뉴 → 빈 대시보드에 (아직 모듈 0개라도) 카드 틀이 뜨는지. `php -l` 로 문법 확인.

**STEP 2~8 — 모듈 구현(기획서 §4 순서):**
1. `header_footer` (뼈대 검증에 가장 적합 — 제일 먼저)
2. `scheduled_publish` (예약글 발행 보장)
3. `auto_index` (IndexNow)
4. `ads_manager` (ads.txt/robots.txt/인증파일)
5. `social_share`
6. `scroll_popup`
7. `ad_protector` (마지막, 통계 브릿지 사용)
- 각 모듈: `includes/modules/class-mod-*.php` + 필요 시 `assets/front/*`. 대시보드 카드에 자동 노출. 독립 토글.

**STEP 9 — 통계 브릿지:** `class-stats-bridge.php` 완성(ad_protector·ads_manager가 사용).

**STEP 10 — 배포 셋업:** `class-updater.php`(통계 것 복사), `readme.txt`, `CLAUDE.md`(§7 템플릿 복사). GitHub repo·릴리스는 **사용자 확인 후**.

---

## 4. 재사용할 원본 (통계 플러그인) — 복붙/참고 대상
경로: `/Users/hupark/클로드작업/wp-visitor-stats/`
- `includes/class-updater.php` → 팩 업데이터의 원본. 상수/저장소명만 교체.
- `includes/class-geo.php` → IP→국가(애드프로텍터 국가차단·허용목록).
- `includes/class-adsense.php` → 애드센스 pub-id/요약(Ads매니저 추천값).
- `includes/class-stats.php` → IP 해시·봇 판정 참고(애드프로텍터 폴백 설계).
- `admin/settings-page.php` → 설정폼 마크업/저장 패턴.
- `wp-visitor-stats.php` → 부트스트랩·훅 등록·활성화훅 구조 참고.
- `CLAUDE.md` → **개발 원칙(원본 우선·땜질 금지) + 배포 절차** 원문(아래 §7에 요지 복사).
> 통계 플러그인이 `class-fm-bootstrap.php`로 다른 플러그인을 전 사이트에 자동배포하는 **허브 패턴**이 있음 → 팩 배포/예약글 핑도 이 패턴을 응용 가능.

---

## 5. 진행 체크리스트 (세션마다 갱신)
- [x] STEP1 스캐폴딩(메인·레지스트리·설정·대시보드·자원)
- [x] 모듈 `header_footer`
- [x] 모듈 `scheduled_publish`
- [x] 모듈 `auto_index`
- [x] 모듈 `ads_manager`
- [x] 모듈 `social_share`
- [x] 모듈 `scroll_popup`
- [x] 모듈 `ad_protector`
- [x] `class-stats-bridge.php`
- [x] `class-updater.php` + `readme.txt` + `CLAUDE.md`
- [ ] **실제 워드프레스에 설치해 동작 검증(미완 — 로컬 WP 환경 없어 `php -l` 문법검사까지만 함)**
- [x] **GitHub repo·첫 릴리스 완료** — `hup5425/wp-site-pack`(비공개), 릴리스 `v0.2.1`. `WSP_UPDATE_REPO` 설정됨. 토큰은 코드가 아니라 **대시보드 입력→DB옵션 `wsp_update_token`**(또는 wp-config `WSP_UPDATE_TOKEN`).
- [x] **저장소 공개 전환 + 전 사이트 자동배포 구축(2026-07-22):**
  - `hup5425/wp-site-pack` **공개(Public)** 로 전환 → 토큰 불필요, 전 사이트 자동설치·자동업데이트.
  - 통계 플러그인에 `includes/class-wsp-bootstrap.php`(=`WVS_WSP_Bootstrap`) 추가 — 파일관리자 부트스트랩과 동일 구조. 통계 **v1.2.67** 릴리스로 배포. 통계가 깔린 전 사이트가 이 업데이트를 받으면 관리자 페이지 로드 시 사이트 팩을 1회 자동 설치·활성화(공개 릴리스 zip). 끄기: `define('WVS_NO_WSP_BOOTSTRAP', true)`.
  - 사이트 팩 자체 업데이트도 공개라 **토큰 없이** 대시보드 버튼으로 동작(과거 만든 DB 토큰 필드·`WSP_UPDATE_TOKEN` 은 공개에선 미사용, 비공개 회귀 대비 잔존).
  - co.coreabiz.com: 소유권(1004 vs 33) 문제로 wp-admin 교체가 실패 → 사용자가 **폴더 수동 삭제 후 재설치**로 해결(이제 33 소유 → 자동업데이트 정상). ⚠ SFTP(2222)는 rate-limit 으로 **차단 지속** — 직접 배포 불가, GitHub 릴리스 경로만 사용.
  - **다음 세션:** 각 사이트가 통계 업데이트(1.2.67)를 실제로 받아 사이트 팩이 깔렸는지 확인. WP 업데이트 체크는 하루 2회라 즉시 아님(수동 트리거 가능).
> **작업 로그(간단히 이어쓰기):**
> - 2026-07-21: 기획서·인계서 작성. 코드 착수 전.
> - 2026-07-22 (2차): **라이브 배포 + 테스트 + 사용자 요청 반영.**
>   - co.coreabiz.com(WP 7.0.2, CloudFront+SFTP:2222)에 SFTP 업로드 → 사용자가 wp-admin 에서 활성화. HTTP 자가진단 스크립트로 검증(전 항목 PASS, 사이트 홈 200 정상).
>   - **버전 0.2.0.** 반영된 사용자 요청:
>     - 설명문에서 "알팩/젯팩 스타일" 문구 제거(wp-site-pack + wp-visitor-stats 둘 다).
>     - auto_index: **빙/네이버 인증 메타**(msvalidate.01 / naver-site-verification) + **전체 URL 일괄 제출**(IndexNow) 추가. 키파일 캐시헤더.
>     - ads_manager: **현재 서빙되는 ads.txt/robots.txt 자동 로드** + 물리파일 write-through + robots 충돌(Rank Math) 경고. 가상 서빙 캐시헤더(CloudFront 1년 캐시 방지).
>     - social_share: **인스타그램·쓰레드·링크복사 버튼** 추가.
>     - scroll_popup: **표시유형(이미지·동영상 배너 / HTML)**, **배너 URL+미디어라이브러리 선택+랜딩 링크**(쿠팡·토스 등), **폭 설정**, **미리보기 버튼**. 애드센스 팝업 금지 안내.
>     - ad_protector: 클릭감지 **창 blur 기법**으로 강화(iframe 광고 대응), 국가코드 2글자 엄격 파싱.
>   - **알아둘 점:** 이 사이트는 **CloudFront(AWS CDN)** 뒤에 있고 `/wp-content/plugins/*` 에 **1년 immutable 캐시**가 걸림. 쿼리스트링은 캐시키에서 무시됨(경로 단위 캐시). → 가상 서빙 응답엔 캐시헤더를 넣었지만, ads.txt/robots.txt/키파일 값 변경이 즉시 반영 안 되면 **CloudFront 무효화(invalidation)** 필요. 또 잔재 폴더 `wp-content/plugins/uploadswvs-update-Smg9cs`(통계 업데이터 옛 버그 흔적) 있음.
>     robots.txt 는 물리파일 아님 = **Rank Math 가 생성**(다음 인증·사이트맵 포함) → ads_manager 로 덮어쓸 때 주의.
> - 2026-07-22 (1차): STEP1 스캐폴딩 + 7개 모듈 전부 1차 구현 완료. 모든 PHP `php -l` 통과.
>   - 프레임워크: `WSP_Module`(추상) / `WSP_Core`(레지스트리, 활성 모듈만 register) / `WSP_Settings`(옵션) / `WSP_Admin`(카드 대시보드+설정 라우팅+POST 저장) / `WSP_Assets`(프론트 자원) / `WSP_Stats_Bridge`(통계 읽기전용) / `WSP_Updater`(통계 것 복사·개조).
>   - 모듈 옵션 `wsp_mod_{slug}`, 활성목록 `wsp_active_modules`. 토글 시 on_activate/on_deactivate + rewrite flush 예약.
>   - ad_protector 전용 테이블 `{prefix}wsp_ad_blocks`(dbDelta). scheduled_publish 외부 트리거 키 자동 생성.
>   - **다음 세션 할 일:** 로컬/실제 WP에 설치 → "사이트 팩" 메뉴로 대시보드·각 모듈 토글·저장 실제 동작 확인. 사용자가 하나씩 검토하며 수정 예정. 미확정 항목(§2)은 아직 사용자 확인 전.

---

## 6. 개발 원칙 (통계 CLAUDE.md에서 계승 — 반드시 준수)
- **원본 우선·땜질 금지:** 문제 생기면 즉흥 코드로 증상만 덮지 말 것. ① 원본을 본다 → ② 원인을 찾는다 → ③ 원본을 고친다/되돌린다 → ④ 정말 안 될 때만 차선책(이유 기록). `width:100%` 식 임시 땜질 금지.
- **한 번 꼬이면 멈추고 되돌린 뒤 다시 시작.** 코드 한 줄 붙이기 전에 "원본은 어땠지?"를 자문.
- 이 팩은 **모듈 격리**가 생명 — 한 모듈 수정이 다른 모듈에 영향 주면 설계가 잘못된 것. 공유 코드는 core/bridge로만.

## 7. 배포 절차 템플릿 (팩용 — CLAUDE.md 만들 때 넣을 것)
- 버전 두 곳 일치: `wp-site-pack.php` 헤더 `Version:` + `WSP_VERSION`.
- 릴리스(가안):
```
cd ~/클로드작업
zip -r wp-site-pack/wp-site-pack-vX.Y.Z.zip wp-site-pack \
  -x "wp-site-pack/.git/*" "wp-site-pack/.claude/*" "wp-site-pack/.DS_Store" \
     "wp-site-pack/*.zip" "wp-site-pack/CLAUDE.md" "wp-site-pack/기획서.md" "wp-site-pack/인계서.md"
cd wp-site-pack && git add -A && git commit -m "vX.Y.Z: ..." && git push
gh release create vX.Y.Z wp-site-pack-vX.Y.Z.zip -t vX.Y.Z -n "변경 내용"
# 릴리스 후 구버전 zip 자동 정리(최신 1개만): diff·릴리스 끝난 맨 마지막에만!
ls -t wp-site-pack-v*.zip | tail -n +2 | xargs -r rm -f
```
- ⚠ zip 만들면 지난 릴리스 zip과 파일목록 diff로 의도한 차이만 있는지 확인.
- ⚠ 구버전 삭제는 **diff·릴리스가 끝난 뒤** 맨 마지막에.

## 8. 이 세션에서 있었던 관련 작업(맥락)
- 직전까지 `wp-visitor-stats`를 다룸: 전체게시글/글목록에 **미니 곡선(스파크라인)** 추가(v1.2.49), 글목록 컬럼 레이아웃·정렬·색 조정(~v1.2.52). 그 과정에서 통계 플러그인 구조를 파악 → 본 팩 설계의 재사용 판단 근거가 됨.
- 사용자가 알팩 6개 기능 스크린샷 제공 → 구조 논의 → **새 모듈형 팩**으로 방향 확정 → "전부 만들고 검토" → **문서 먼저** 요청(이 문서들).
- 이후 사용자 추가 요청으로 **예약글 발행 보장** 모듈(7번) 편입.
