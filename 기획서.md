# wp-site-pack 기획서

> **한 줄 요약:** 알팩(AlPack)처럼 여러 사이트 운영 유틸리티를 **모듈 On/Off** 방식으로 한 플러그인에 담는 **모듈형 통합 팩**. 통계 플러그인(wp-visitor-stats)은 그대로 두고, 겹치는 기능은 통계 데이터를 **다리(bridge)** 로 재사용한다.
>
> 상태: **기획 단계 (코드 0줄)** · 작성일 2026-07-21 · 작성자 Claude(+사용자 박형욱)
> 폴더명 `wp-site-pack` 은 **가칭** — 브랜드명 확정 시 일괄 변경(아래 "미확정 항목" 참조).

---

## 1. 배경 · 목표

- 참고 제품: **알팩(AlPack)** — https://alpack.dev/ , 저자 loword. 통계 + 여러 유틸을 하나로 묶은 통합 플러그인.
- 사용자는 이미 여러 워드프레스 플러그인을 단독 유지 중: `wp-visitor-stats`(통계·PWA 앱 포함), `safe-file-manager`, `링크 체크`, `wp-storage-optimizer`, `wp-auto-writer`, `naver-auto-writer`, `keyword-tool`, `wp-full-migrator`, `apt-bill-view`.
- **목표:** 알팩의 아래 6개 기능을 자체 제작해, 새 **모듈형 팩** 플러그인으로 제공한다. 장기적으로 작은 유틸(링크체크·용량최적화 등)도 이 팩의 모듈로 흡수해 **관리 창구를 하나로** 줄인다.

### 왜 "새 모듈형 팩"인가 (결정 근거)
- 통계 플러그인은 DB·REST·전용 PWA 앱까지 딸린 **큰 측정 제품** → 프론트 유틸(팝업·소셜) 코드를 섞으면 하나 고치다 통계가 깨질 위험(사용자가 강하게 금지한 "땜질 연쇄"). → 성격 분리.
- 알팩의 핵심은 "우겨넣기"가 아니라 **기능별 On/Off + 독립 설정(모듈 구조)**. 켠 모듈만 로드 → 가볍고, 한 모듈 오류가 다른 데로 안 번짐. 사용자의 "원본 우선·땜질 금지" 원칙과 부합.
- 통계는 이미 모든 사이트에 설치·자동업데이트되고 있음(그리고 `class-fm-bootstrap.php`로 파일매니저까지 배포하는 **허브** 역할). → 팩은 통계의 IP·지역·애드센스 데이터를 재사용하면 중복 없음.

---

## 2. 모듈 목록 (알팩 스크린샷 기준)

| # | 모듈명(한글) | 슬러그 | 성격 | 통계 연동 | 난이도 |
|---|---|---|---|---|---|
| 1 | 헤더 & 푸터 | `header_footer` | 프론트 코드 삽입 | 없음 | ★☆☆ |
| 2 | 자동 인덱싱(IndexNow) | `auto_index` | SEO | 없음 | ★★☆ |
| 3 | Ads 매니저 | `ads_manager` | 광고/파일 | class-adsense 참고 | ★★☆ |
| 4 | 소셜 공유 | `social_share` | 프론트 | 없음 | ★★☆ |
| 5 | 스마트 스크롤(팝업) | `scroll_popup` | 프론트 | 없음 | ★★☆ |
| 6 | 애드 프로텍터 | `ad_protector` | 광고 보호 | **IP·지역·봇 재사용(핵심)** | ★★★ |
| 7 | 예약글 발행 보장 | `scheduled_publish` | 운영/안정성 | 없음(단독 동작) | ★★☆ |
| 8 | 관련 글 | `related_posts` | 프론트 | 없음 | ★★☆ |
| 9 | **SEO** (2026-09-14 추가) | `seo` | SEO — **Rank Math 를 대신한다** | 없음 | ★★★ |

> **권장 제작 순서:** 1 헤더&푸터(뼈대 검증) → 2 예약글 발행 보장 → 3 자동인덱싱 → 4 Ads매니저 → 5 소셜공유 → 6 스마트스크롤 → 7 애드프로텍터(가장 복잡, 마지막). 사용자 요청은 "일단 전부 다 만들고 하나씩 검토"이므로, 아래 순서대로 완성하되 각 모듈을 독립적으로 켜고 끌 수 있게 만든다.
>
> ※ 7번 예약글 발행 보장은 순서상 앞쪽에 두어도 됨(단순·유용). 통계 연동 불필요.

---

## 3. 아키텍처

### 3.1 폴더 구조(예정)
```
wp-site-pack/
  wp-site-pack.php          # 메인: 상수, 오토로드, 부트스트랩, 관리자 메뉴
  includes/
    class-core.php          # 모듈 레지스트리(활성 모듈만 register() 호출)
    class-module.php         # 추상 클래스(모듈 공통 인터페이스)
    class-settings.php       # 옵션 저장/조회 헬퍼(모듈별 옵션)
    class-admin.php          # 대시보드(카드 그리드) + 설정 페이지 라우팅
    class-assets.php         # 관리자/프론트 자원 로드
    class-stats-bridge.php   # wp-visitor-stats 데이터 재사용(IP·지역·애드센스)
    class-updater.php        # GitHub 릴리스 자동 업데이트(통계 것 복사·개조)
    modules/
      class-mod-header-footer.php
      class-mod-auto-index.php
      class-mod-ads-manager.php
      class-mod-social-share.php
      class-mod-scroll-popup.php
      class-mod-ad-protector.php
      class-mod-scheduled-publish.php
  admin/
    dashboard.php           # 카드 대시보드 뷰
    settings-*.php          # 모듈별 설정 폼(또는 각 모듈 클래스가 렌더)
  assets/
    admin.css / admin.js    # 대시보드·토글·설정 공용 UI
    front/*.css /*.js       # 소셜공유·팝업 등 프론트 자원(모듈별)
  languages/                # (선택) 번역
  readme.txt
  CLAUDE.md                 # 개발 원칙 + 배포 절차(통계 것과 동일 톤)
```

### 3.2 모듈 프레임워크(핵심 설계)
- **추상 클래스 `WSP_Module`** 가 공통 계약을 정의:
  - `id()` 슬러그, `name()` 표시명, `desc()` 설명(카드 문구), `icon()`(선택)
  - `default_settings()` 기본값
  - `register()` — **활성 상태일 때만** 실제 훅(프론트/관리자 동작) 등록
  - `render_settings()` — 설정 페이지 폼 렌더
  - `sanitize( $input )` — 저장 전 검증
- **레지스트리 `WSP_Core`**: 모든 모듈을 목록에 등록 → 활성(active) 모듈만 `register()` 실행. 비활성 모듈은 런타임 코드가 **아예 안 걸림**(성능·격리).
- **대시보드**: 등록된 모듈들을 카드 그리드로 나열(활성화됨/비활성화됨 배지 + 이름 + 설명 + [설정] 버튼). 알팩 스크린샷과 동일한 UX.

### 3.3 데이터 저장
- **모듈별 옵션 분리**를 원칙으로: `wsp_mod_{slug}` (예: `wsp_mod_ad_protector`). 각 모듈이 독립적으로 저장/삭제 → 한 모듈 문제로 다른 설정이 오염되지 않음.
- **활성 목록**: `wsp_active_modules` = `['header_footer'=>1, 'ad_protector'=>1, ...]` (또는 각 모듈 옵션 안에 `active` 플래그). → **활성 목록 단일 옵션**을 권장(대시보드 토글이 여기만 갱신).
- 무거운 데이터(차단 로그, 인덱싱 로그)는 **전용 테이블 또는 옵션 캡** 사용(무한 증가 방지). 애드프로텍터 차단 로그는 커스텀 테이블 권장.

### 3.4 통계 브릿지(`class-stats-bridge.php`)
- `wp-visitor-stats` 활성 여부 감지(`class_exists('WVS_Stats')` 또는 함수 존재 체크).
- 있으면: 방문자 IP 해시·국가(class-geo)·봇 판정·애드센스 요약을 **읽어서** 재사용.
- 없으면: 각 모듈이 **자체 최소 기능**으로 동작(예: 애드프로텍터는 자체 IP 수집, Ads매니저는 애드센스 요약 없이 파일관리만).
- 원칙: **통계 DB에 쓰지 않는다(읽기 전용).** 통계는 측정 전용으로 보존.

### 3.5 배포/업데이트
- 통계와 동일 방식: **GitHub 저장소 + 릴리스 zip + 자체 업데이터**(`class-updater.php`는 wp-visitor-stats의 것을 복사해 상수명만 교체).
- 버전 두 곳 일치: `wp-site-pack.php` 헤더 `Version:` + `WSP_VERSION` 상수.
- **구버전 zip 자동 정리**(통계 배포 절차와 동일): 릴리스 후 최신 1개만 남김.

---

## 4. 모듈별 상세 명세

> 각 설정 페이지 상단에는 공통으로 **"플러그인 기능 활성화" 토글**이 있고(대시보드 카드 배지와 연동), 하단에 **저장하기** 버튼. 알팩 UI와 동일한 행(row) 레이아웃.

### 4.1 헤더 & 푸터 `header_footer`  (가장 단순 — 뼈대 검증용)
- **입력 4곳(각 textarea, 원시 코드 허용):**
  1. 헤더 코드 — `wp_head` 훅에 출력(`<head>` 내부)
  2. Body 시작 코드 — `wp_body_open` 훅(`<body>` 직후)
  3. Body 종료 전 코드 — `wp_footer` 앞쪽(우선순위 조절)
  4. 푸터 코드 — `wp_footer` 훅
- 코드는 관리자만 입력 → **이스케이프하지 않고 그대로 출력**(단, `unfiltered_html`/`manage_options` 권한 체크). 저장 시 nonce·권한만 확인.
- "코드 저장 후 웹사이트에서 즉시 반영".
- **주의:** 캐시 플러그인 있으면 캐시 비움 안내. XSS는 관리자 자기 책임 영역.

### 4.2 자동 인덱싱(IndexNow) `auto_index`
- **탭 3개:** 설정 / 로그 / 수동 인덱싱.
- 설정:
  - 기능 활성화 토글
  - **IndexNow API 키**(예: `naver-9a9284E-...`). 키 파일을 사이트 루트에서 서빙해야 함(`https://site/{key}.txt` 가 키 문자열 반환).
  - 자동 인덱싱 활성화 토글(게시글 발행/수정 시 자동 ping)
  - 인덱싱 대상: 게시글 / 페이지(칩 토글)
- 동작: `transition_post_status`(publish 진입) 또는 `save_post` 시 IndexNow 엔드포인트로 URL 제출(네이버/Bing 등 IndexNow 지원 엔진). WP-Cron/백그라운드로 비동기 처리(페이지 안 막음).
- **키 파일 서빙:** 물리 파일 대신 **가상 서빙**(rewrite + `template_redirect`)을 우선(파일 쓰기 권한 불필요). `{key}.txt` 요청 시 키 텍스트 응답.
- 로그: 최근 제출 URL·시각·응답코드(테이블 또는 옵션 캡 100건).
- 수동 인덱싱: URL 직접 입력 후 즉시 제출.
- 안내문: "IndexNow는 요청을 보낼 뿐, 인덱싱 결과를 보장하지 않습니다."
- **(2026-09-14 추가) 구글 인덱싱 API** — 사장님이 쓰시던 Rank Math **Instant Indexing** 플러그인을 대신한다(그 플러그인은 삭제). 설정 탭에 「구글 서비스 계정 키(JSON)」 붙여넣기 칸 + 「구글 인덱싱 API 자동 전송」 체크. 글 발행·수정 → `URL_UPDATED`, 삭제·비공개 → `URL_DELETED`. 하루 200건 한도라 그날 보낸 수를 세어 화면에 보이고 넘으면 다음 날로 미룬다. 토큰은 서비스 계정 키로 JWT(RS256, `openssl_sign`)를 만들어 받고 55분 캐시. 로그는 IndexNow 와 같은 로그 탭에 「구글」 표시로. 수동 인덱싱 탭의 단추도 IndexNow·구글 둘 다 보낸다(각각 켜져 있을 때만).
- **빙**: 사장님이 쓰시던 **Bing Webmaster Url Submission** 플러그인은 IndexNow 로 대체된다(빙 자신이 IndexNow 로 넘어감). 따로 만들지 않고 삭제한다.
- **MWW Scheduled Post Trigger** 는 4.7 예약글 발행 보장 모듈이 대신한다(삭제). zau.kr 에서 예약글이 실제로 시각에 나가는지 확인한 뒤 나머지 사이트에서 지운다.

### 4.3 Ads 매니저 `ads_manager`
- **현재 파일 상태:** ads.txt 존재 여부 / robots.txt 존재 여부(체크 아이콘).
- **ads.txt 내용 편집**(textarea) — 예: `google.com, pub-XXXX, DIRECT, f08c47fec0942fa0`.
- **robots.txt 내용 편집**(textarea) + **[기본 값 설정]** 버튼(기본 robots 템플릿 채움).
- **네이버 서치어드바이저 인증 업로드**(HTML 파일 업로드).
- **구글 서치콘솔 인증 업로드**(HTML 파일 업로드).
- 동작:
  - `robots.txt` → 워드프레스 `robots_txt` 필터로 내용 치환(가상). 물리 robots.txt 있으면 그게 우선이라 안내 필요. 알팩은 "Rank Math SEO 있어도 우선순위"라고 표기 → 우리도 필터 우선순위 높게.
  - `ads.txt` → 루트 `/ads.txt` 요청을 가상 서빙(rewrite) 또는 물리 파일 생성(권한 있으면). **가상 서빙 우선.**
  - 인증 HTML 파일 → 루트 경로에서 접근돼야 함 → 가상 서빙 또는 물리 업로드(권한 필요). 업로드 파일명/내용 저장 후 해당 경로 요청 시 응답.
- 통계 연동(선택): class-adsense가 있으면 애드센스 pub-id 자동 추천.

### 4.4 소셜 공유 `social_share`
- 기능 활성화 토글.
- **활성화할 소셜 미디어**(칩 토글): 페이스북 / 밴드 / 카카오톡 / 네이버 / 라인 / X. (카카오톡은 API 키 없으면 비활성 표시.)
- **카카오톡 API KEY** 입력(카카오 JS 키). 없으면 카카오 공유 비활성.
- **소셜 공유 스타일**: 나열 형식 등(드롭다운).
- **버튼 정렬**: 왼쪽/가운데/오른쪽(드롭다운).
- **숏코드 복사**: 자체 숏코드 제공(예: `[wsp_social_share]`). (알팩은 `[presslearn_social_share]` — 우리 접두어로.)
- 동작: 숏코드 렌더 + (옵션) `the_content` 자동 삽입. 각 SNS 공유 URL 스킴. 카카오는 Kakao SDK 로드 후 `Kakao.Share`.

### 4.5 스마트 스크롤(팝업) `scroll_popup`
- 기능 활성화 토글. **단일 캠페인만 운영**(하나의 팝업).
- **스크롤 퍼센트**(숫자, 예 50): 페이지의 N%까지 스크롤하면 팝업 표시.
- **팝업 애니메이션**: Fade 등(드롭다운).
- **반복 설정**: "브라우저 당 한 번만 표시" 등(드롭다운) → 쿠키/localStorage로 빈도 제어.
- **팝업 표시 내용**: [내용 설정하기] → HTML 에디터(팝업 본문). 미리보기 지원.
- 동작: 프론트 JS가 스크롤 감지 → 조건 충족 시 팝업. 빈도 규칙 저장. 저장 후 미리보기 클릭으로 확인.

### 4.6 애드 프로텍터 `ad_protector`  (가장 복잡 — 마지막)
- 기능 활성화 토글.
- **최대 허용 클릭 수**(예 3) + **클릭 감지 시간(분)**(예 30): 한 IP가 시간창 내 광고를 N회 초과 클릭하면 차단.
- **CloudFlare 사용** 토글: CF API로 엣지 차단(토큰 필요). 없으면 자체 차단(광고 숨김/모달).
- **허용 IP** / **차단 IP** / **차단 국가** 설정(모달 목록 편집).
- **차단 IP 해제 주기(일)**(예 30): 자동 해제.
- **차단 모달 설정**: 차단된 사용자에게 보여줄 화면.
- **차단 로그 확인**: 차단 이력 테이블.
- 동작:
  - 프론트 JS가 애드센스 광고 영역(`ins.adsbygoogle`) 클릭을 감지 → AJAX로 카운트 기록(IP 해시 기준, 시간창).
  - 초과 시 해당 IP를 차단 목록에 추가 → 이후 광고 미표시/모달.
  - 국가 차단·허용목록은 **통계의 class-geo(IP→국가)** 재사용.
  - CF 연동은 옵션(토큰 저장, API로 IP 룰 추가).
  - 차단 로그·차단 IP는 **전용 테이블** 권장(`{prefix}wsp_ad_blocks`).
- 안내문: "애드 프로텍터는 광고 차단의 무결성을 보장하지 않습니다."
- **통계 재사용 핵심:** IP 해시·국가·봇 판정을 통계에서 읽어 중복 수집 최소화(브릿지). 통계 없으면 자체 IP 수집으로 폴백.

### 4.7 예약글 발행 보장 `scheduled_publish`  (MWW Scheduled Post Trigger 대체)
- **문제:** 워드프레스 예약 발행은 **WP-Cron**에 의존하는데, WP-Cron은 **사이트에 방문(요청)이 있을 때만** 실행된다. 방문자가 없으면 예약 시각이 지나도 글이 `future`(예약) 상태로 **"발행 누락(missed schedule)"** 되어 안 올라간다. 사용자는 현재 이를 **MWW Scheduled Post Trigger**로 해결 중.
- **해결(우리 모듈):** 방문자 유무와 무관하게 예약 시각이 지난 글을 **강제로 발행**한다. 세 겹 안전망:
  1. **외부 트리거 엔드포인트** — `https://site/?wsp_publish_due={KEY}` (또는 REST `wsp/v1/publish-due`) 호출 시, `future` 상태이면서 `post_date <= 현재`인 글을 찾아 `wp_publish_post()`로 발행. → **실서버 크론(crontab)이나 외부 핑 서비스, 또는 허브(apt-view.com)가 주기적으로 이 URL을 때려** 방문자 없이도 보장. (통계 플러그인이 이미 모든 사이트에 깔려 배포 허브 역할을 하므로, **허브가 등록 사이트들의 이 엔드포인트를 주기적으로 핑**하는 방식이 가장 확실 — 장기 옵션.)
  2. **자체 WP-Cron 재등록** — 짧은 주기(예: 5~15분) 커스텀 cron 이벤트로 누락분 발행 시도(방문이 조금이라도 있으면 촘촘히 보완).
  3. **요청 시 보정** — `wp_loaded`/`init`에서 누락된 예약글을 가볍게 훑어(게이트로 과부하 방지) 발행. (MWW 방식과 동일 — 방문 있을 때 즉시 보정.)
- **설정 페이지:**
  - 기능 활성화 토글
  - **트리거 방식 선택:** 자체 크론 / 외부 핑(엔드포인트 URL·키 표시, 복사 버튼) / 둘 다
  - **점검 주기(분)** (자체 크론용, 예: 10)
  - **트리거 URL·비밀 키** 표시(외부 크론·핑 설정용) + 재발급 버튼
  - **최근 발행 로그**(누락 보정으로 발행된 글·시각, 옵션 캡)
  - (선택) **실서버 크론 등록 안내문**(crontab 예시: `*/10 * * * * curl -s 'https://site/?wsp_publish_due=KEY' >/dev/null`)
- **동작 상세:**
  - 발행 쿼리: `post_status='future'` AND `post_date_gmt <= 현재(GMT)` → `wp_publish_post( $id )` (published 로 전환, 예약 훅·핑 정상 발생).
  - 엔드포인트는 **키 검증** 후 즉시 처리(권한 불필요 — 키가 비밀). 처리량 캡·중복 방지.
  - `DISABLE_WP_CRON` 환경(실크론 사용 사이트)에서도 외부 엔드포인트로 정상 동작.
- **통계 연동:** 없음(완전 단독). 단, 배포 허브의 주기적 핑을 쓰면 통계 플러그인의 배포/스케줄 인프라와 함께 관리하면 편함.
- **주의:** MWW와 동시 활성 시 중복 발행 시도가 겹칠 수 있으나 `wp_publish_post`는 멱등(이미 publish면 무해). 전환 시 MWW 비활성 권장.

### 4.8 SEO `seo`  (2026-09-14 설계 · Rank Math 무료·프로를 대신한다)

> **왜:** 네 사이트(coreabiz·apt-view·benefitf·zau.kr)가 Rank Math 무료판+프로판 두 개를 깔고 있었는데,
> 실제로 쓰는 것은 아래 목록뿐이다(2026-09-14 공개 페이지 실측). 구글은 FAQ 표시를 2026-05-07 에
> 끝냈지만 구조화 데이터 자체는 봇·AI 가 읽기 좋아 **계속 붙인다**(사장님 결정).
> 알팩·Rank Math 는 이 모듈이 검증된 뒤 **전부 삭제**한다. 시험은 zau.kr 에서 전부 하고 나머지 세 사이트에 배포.

**원칙**
- **Rank Math 가 켜져 있으면 이 모듈은 머리말(head)에 아무것도 내보내지 않고** 설정 화면에 "Rank Math 가 켜져 있어 이 모듈이 쉬고 있습니다 — 하나만 켜세요" 를 보인다. 같은 태그가 두 번 나가면 안 된다. 사이트맵·첨부파일 처리도 같이 쉰다.
- **robots.txt 는 Ads 매니저**, **인증 메타·인증 파일·IndexNow 는 자동 인덱싱** 이 맡는다. 이 모듈은 건드리지 않는다(같은 일을 두 곳에 두지 않는다).
- 페이지 캐시(Breeze+CDN)가 있으므로 **방문자마다 달라지는 값을 HTML 에 넣지 않는다.**
- 화면 이름과 저장 이름은 같은 낱말. 설정 화면에 없는 동작을 코드에만 두지 않는다.
- 값을 읽을 때 `the_content` 필터 결과를 쓰지 않는다(관련 글·소셜 공유가 거기 붙는다). **원본 `post_content`** 를 읽는다.

**(가) 제목 태그**
- `document_title_parts` 필터. 글·페이지에서는 **사이트명을 붙이지 않는다**(지금 Rank Math 동작). 설정: 「글 제목 뒤에 사이트명 붙이기」 체크(기본 끔), 「구분 기호」(기본 `-`).
- 홈: 사이트명 (+ 태그라인이 있으면 `사이트명 - 태그라인`). ※ 지금 Rank Math 는 zau.kr 홈을 `다시쓰기 -` 로 내보낸다(태그라인이 비어 있는데 구분 기호를 붙임). 우리는 비면 안 붙인다.
- 글마다 「검색 제목」을 따로 적었으면 그것을 쓴다(아래 (사)).

**(나) 설명문(메타 디스크립션)**
- 우선순위: 글의 「설명문」(우리 메타 `_wsp_seo_description`) → Rank Math 가 저장해 둔 `rank_math_description`(읽기만, 옮겨 쓰지 않음) → 손으로 쓴 요약(excerpt) → **본문 첫 문장부터 160자**(Rank Math 기본 160자, 사장님 말씀 "첫 문장 자동"). 본문은 원본 `post_content` 에서 블록 주석·숏코드·태그를 걷어 낸 것. 160자 안에서 문장 끝(`. ! ? 다.`)이 있으면 거기서 자른다.
- 홈: 「사이트 설명문」 설정값 → 없으면 태그라인. 카테고리: 카테고리 설명 → 없으면 안 내보냄.

**(다) 로봇 메타** — 워드프레스 표준 `wp_robots` 필터로.
- 글·페이지·카테고리·홈: `index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1` (지금과 같음. `max-image-preview:large` 가 빠지면 디스커버에 사진이 작게 나간다).
- 사이트 안 검색(`?s=`)·작성자 페이지·첨부파일 페이지·404: `noindex, follow` (지금과 같음). 설정: 「작성자 페이지 검색 노출」 체크(기본 끔).
- canonical 은 워드프레스 기본(`rel_canonical`)을 그대로 둔다. Rank Math 가 없어지면 기본이 돌아온다 — 대조 도구로 값이 같은지 확인.

**(라) 공유 태그(오픈그래프·트위터 카드)** — 사장님이 말씀하신 "SNS 공유 때 나오는 박스".
- `og:locale`(ko_KR) · `og:type`(글=article, 그 외=website) · `og:title` · `og:description` · `og:url` · `og:site_name` · `article:published_time` · `article:modified_time` · `article:section`(첫 카테고리)
- `og:image` + `og:image:width/height/alt/type` + `og:image:secure_url`: 대표사진(원본 크기) → 없으면 본문 첫 사진 → 없으면 설정 「기본 공유 사진」(홈에도 이것) → 그것도 없으면 사이트 아이콘.
- `twitter:card`(summary_large_image) · `twitter:title` · `twitter:description` · `twitter:image`. Rank Math 의 `twitter:label1/data1`(작성자)·`label2/data2`(읽을 시간)은 **넣지 않는다**(효과 없음).

**(마) 구조화 데이터(JSON-LD, `<script type="application/ld+json">` 하나에 `@graph`)**
- 사이트 공통: `WebSite`(name, url, alternateName=「사이트 다른 이름」), **`Organization`**(name=「조직 이름」 기본 사이트명, logo=「로고」 기본 사이트 아이콘, `sameAs`=「연관 채널 주소」 줄마다 하나 — 네이버 블로그 등). ※ 지금은 발행자가 `Person` 인데 조직+로고로 바꾸면 검색결과 사이트 이름·아이콘에 유리하다.
- 글: `WebPage`(url, name, isPartOf, datePublished, dateModified, primaryImageOfPage) + **글 종류**(설정 「글 종류」: `BlogPosting`(기본) / `Article` / `NewsArticle` — 지금 coreabiz 는 NewsArticle, 나머지는 BlogPosting 이니 옮길 때 그대로 맞춘다) with headline·datePublished·dateModified·author(`Person`, name=「작성자 표시 이름」 기본 작성자 표시명)·publisher(Organization)·image(대표사진, 1200px 이상이면 그대로)·mainEntityOfPage·articleSection·wordCount·description(=(나)).
- `ImageObject`(대표사진 url·width·height).
- **`FAQPage`** — 두 곳에서 찾는다. ①옛 `rank-math/faq-block` 블록의 속성 JSON(`questions[].title/content/visible`) — 이미 발행된 약 3,600편이 이 모양이라 반드시 읽는다. ②제목 글자가 `자주 묻는 질문`·`자주묻는질문`·`FAQ` 인 H2/H3 아래의, 한 단계 아래 제목(질문)+그 뒤 문단(답). wp-auto-writer 가 앞으로 특정 플러그인 블록 대신 이 모양으로 쓰기 위해서다(`src/services/assembler.py` `apply_faq_schema` 와 같은 규칙). 답의 HTML 은 태그를 걷어 평문으로. 질문이 0개면 안 붙인다.
  - 앞면 모양: 옛 블록의 `.wp-block-rank-math-faq-block` 이 Rank Math CSS 없이도 깨지지 않게 최소 CSS 를 준다(질문 굵게·간격). 편집 화면에서 옛 블록은 '지원하지 않는 블록'으로 보이지만 내용은 그대로 남는다 — 3,600편을 고쳐 쓰지 않는다.
- **`VideoObject`** — 본문에 유튜브(`youtube.com/embed`·`youtu.be`·`core/embed` youtube)가 있을 때만: name=글 제목, description=(나), thumbnailUrl=`https://i.ytimg.com/vi/{id}/hqdefault.jpg`, uploadDate=발행 시각, embedUrl. 없으면 안 붙인다(최신 글 20편에 동영상 0).
- `BreadcrumbList`(홈 › 카테고리 › 글): 설정 「경로 표시」 체크(기본 켬). 화면 표시는 하지 않는다(데이터만).
- ❌ 넣지 않는 것: 글 별점·평점(자기 평가는 구글 지침 위반, 처벌 시 사이트 전체 표시가 꺼진다), HowTo(2023 종료).

**(바) 사이트맵** — 주소를 **지금 그대로** 유지한다(검색콘솔·네이버·다음에 등록된 주소가 끊기면 안 된다).
- `/sitemap_index.xml` → `post-sitemap1.xml … N`, `page-sitemap.xml`, `category-sitemap.xml`. 한 파일에 **200편**(설정 「사이트맵 한 파일에 글 수」 기본 200 — 바꾸면 파일 번호가 달라지니 안내). 글은 오래된 것부터 차례로(Rank Math 와 같은 순서라 기존 파일 번호가 유지된다).
- 항목: `<loc>` + `<lastmod>`(수정 시각, ISO 8601) + `<image:image>`(대표사진). 첨부파일·noindex 글·비공개는 뺀다. 카테고리는 글이 1편 이상인 것만.
- 워드프레스 기본 사이트맵은 끈다(`wp_sitemaps_enabled` false). `/wp-sitemap.xml`·`/sitemap.xml` 은 `/sitemap_index.xml` 로 **301**(지금과 같음).
- 서빙: rewrite 규칙 + `template_redirect`. **`status_header(200)` · `Content-Type: application/xml; charset=UTF-8` · `nocache_headers()` 를 반드시.** 모듈을 켤 때 `flush_rewrite_rules`.
- 만드는 비용: 파일마다 트랜지언트 1시간, 글 발행·수정·삭제·카테고리 변경 때 그 파일만 지운다. 글 1,400편이면 파일 7개라 가볍다.
- 사이트맵 핑은 하지 않는다(구글은 2023 년에 핑을 끝냈다. 즉시 색인은 자동 인덱싱 모듈의 IndexNow 가 한다).

**(사) 스니펫 편집기 + 포커스 키워드** — 글 편집 화면 아래 메타박스 「검색 미리보기」. 사장님이 손으로 글을 쓰실 때 쓰신다.
- 칸: 「검색 제목」(비면 글 제목) · 「설명문」(글자 수 표시, 160 넘으면 빨갛게) · 「주소」(슬러그, 저장하면 `post_name` 이 바뀐다. 발행된 글의 주소를 바꾸면 옛 주소는 워드프레스가 스스로 새 주소로 보낸다는 안내 한 줄) · 「포커스 키워드」.
- 미리보기: 구글 검색결과 모양(제목·주소·설명문)을 입력하는 대로 바꿔 보인다(JS).
- 키워드 세기: 「다시 세기」 단추 + 화면 열 때 한 번. 편집 중인 본문은 `wp.data.select('core/editor').getEditedPostContent()` 로 읽는다(구텐베르크 화면 안에서 도는 메타박스라 가능). 표: 제목 / 주소 / 설명문 / 첫 문단 / 소제목(H2·H3) / 본문 전체 — 각각 **몇 번** 들어갔나. 점수·색 등급은 만들지 않는다(순위와 무관).
- 저장: `_wsp_seo_title` · `_wsp_seo_description` · `_wsp_seo_focus_keyword`. 셋 다 `register_post_meta(... show_in_rest=true, auth_callback=edit_post)` 로 REST 에 열어 **wp-auto-writer 가 발행할 때 `meta` 로 같이 보낼 수 있게** 한다(워프 쪽 변경은 따로).
- 옛 값 이어 받기: 우리 메타가 비어 있으면 `rank_math_title` · `rank_math_description` · `rank_math_focus_keyword` 를 **읽어서** 칸에 보여 준다(저장을 누르면 그때 우리 메타로 들어간다). 한꺼번에 옮기지 않는다.
  ⚠️ Rank Math 를 삭제할 때 **"삭제 시 데이터도 지움" 설정을 꺼 둔 채** 지워야 이 값이 남는다 — 사장님께 안내.

**(아) 첨부파일 페이지** — 설정 「첨부파일 페이지」: `글로 보내기`(기본: 부모 글로 301, 부모 없으면 홈) / `워드프레스 기본`. 지금은 benefitf·zau 가 홈으로 301, coreabiz·apt-view 는 404 라 사이트마다 달랐다 — 통일한다.

**설정 화면(행 레이아웃, 위→아래)**: 활성화 토글 · [Rank Math 겹침 경고] · 제목(사이트명 붙이기·구분 기호) · 사이트 설명문 · 기본 공유 사진 · 조직 이름·로고·사이트 다른 이름·연관 채널 주소 · 글 종류 · 작성자 표시 이름 · 경로 표시 · 작성자 페이지 검색 노출 · 사이트맵 한 파일에 글 수 + 「지금 사이트맵 보기」 링크 · 첨부파일 페이지 · [저장]. `sanitize()` 가 열쇠를 하나라도 빠뜨리면 그 값이 사라지므로 기본값 목록과 1:1 로 맞춘다.

**대조 도구 `tools/대조.py`** (파이썬 표준 라이브러리만) — Rank Math 를 끄기 **전**과 **후**를 자동으로 견준다.
- 입력: 사이트 주소. 훑는 곳: 홈 · 최신 글 5편(REST 로 얻음) · 카테고리 1곳 · 페이지 1곳 · `?s=test` · 작성자 페이지 · 첨부파일 1곳 · `/robots.txt` · `/sitemap_index.xml` 과 그 안의 모든 사이트맵 · `/wp-sitemap.xml`.
- 캐시를 피하려고 모든 요청에 `?wsp_nocache=<시각>` 을 붙인다(zau.kr 실측: 쿼리가 붙으면 `x-cache: MISS`). 응답에 `age` 가 0 이 아니면 경고.
- 기록하는 것: 상태 코드·리다이렉트 목적지 · `<title>` · description · robots · canonical · og:* · twitter:* · JSON-LD 의 `@type` 마다 주요 값(headline·datePublished·dateModified·publisher 종류·image·질문 수) · 사이트맵의 주소 집합과 lastmod.
- `저장 전.json` / `저장 후.json` 으로 남기고 `--diff 전 후` 로 다른 것만 표로 보인다. **의도한 차이**(twitter:label 삭제·publisher Person→Organization·홈 제목 `-` 제거)는 도구가 "예상된 차이"로 따로 묶는다.

**옮기는 순서(zau.kr 부터)**: ① 대조 도구로 「전」 저장 → ② 사이트팩 설치·SEO 모듈 켬(Rank Math 가 켜져 있어 쉼) → ③ Rank Math 무료·프로 끔(삭제 아님) → ④ 「후」 저장·diff → 차이가 예상된 것뿐이면 ⑤ Rank Math 삭제(데이터 지움 설정 끈 채) → ⑥ 나머지 세 사이트 같은 순서. 알팩은 apt-view 에서 같은 때 삭제.

---

## 5. 공통 UI/UX 규칙
- 관리자 메뉴 최상위: **"사이트 팩"**(가칭) → 하위: 대시보드 + 각 모듈 설정.
- 대시보드 = 알팩식 **카드 그리드**(활성/비활성 배지, 설명, [설정]).
- 설정 페이지 = **행(row) 레이아웃**(좌: 라벨/설명, 우: 컨트롤), 상단 활성화 토글, 우하단 [저장하기].
- 저장은 nonce + `manage_options` 권한. AJAX 또는 폼 POST(단순하게 폼 POST 우선).
- 스타일은 통계 플러그인 톤(파랑 #2271b1 계열)과 워드프레스 관리자 기본을 따름. 무거운 프레임워크(React) 미사용 — PHP 렌더 + 소량 JS/CSS.

---

## 6. 미확정 항목 (사용자 결정 필요)
- [ ] **브랜드/플러그인명** 확정(폴더 `wp-site-pack`·접두어 `wsp_`·상수 `WSP_`·메뉴명 "사이트 팩" 모두 가칭). 확정 시 일괄 치환.
- [ ] **GitHub 저장소** 신규 생성 여부(자동 업데이트용). 통계처럼 비공개+토큰 방식?
- [ ] **배포 대상**: 통계처럼 모든 사이트 자동배포? 아니면 수동 설치?
- [ ] 각 모듈에 필요한 **외부 키**: IndexNow 키, 카카오 JS 키, CloudFlare API 토큰 — 누가/언제 입력.
- [ ] robots.txt/ads.txt/인증파일 — **가상 서빙만으로 충분한지**(대부분 충분) vs 물리 파일 필요 사이트 대응.
- [ ] 링크체크·용량최적화 등 기존 유틸을 **이 팩으로 흡수할지**(장기).

---

## 7. 참고 링크
- 알팩: https://alpack.dev/ , https://profiles.wordpress.org/loword/
- IndexNow: https://www.indexnow.org/
- 통계 플러그인 재사용 대상: `wp-visitor-stats/includes/` 의 `class-updater.php`(업데이터), `class-geo.php`(국가), `class-adsense.php`(애드센스), `class-stats.php`(IP/봇), 설정폼 패턴 `admin/settings-page.php`.
